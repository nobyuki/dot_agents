---
name: impl-driver
description: >
  計画書に基づいてコード実装を進めるペアプログラミングのドライバースキル。
  ユーザーが「実装して」「Step N に進んで」「作業を開始して」と言ったとき、
  計画書の実装ステップに沿ってコード変更・テスト・コミットを行う。
  ユーザーはナビゲーター役として方針判断・コミット承認を担う。
  また「計画書を更新して」「計画書に反映して」「計画書を実装に合わせて」と言われたときにも使う。
  実装結果やレビュー対応の結果を計画書に追従させる更新もこのスキルの役割。
  仕様ドキュメントの更新計画作成は plan-doc-update を使うこと。
---

# ペアプロ・ドライバー

## 前提メモリ

スキル実行前に以下の Serena メモリを読み込むこと:

| メモリ名 | 用途 |
|---------|------|
| `project/plan_docs_location` | 計画書の保存先パスとコミットルール |
| `project/architecture_knowledge` | 開発コマンド（typecheck, test, lint） |

計画書の実装ステップに沿ってコードを変更し、テスト・コミットまで一貫して進めるスキル。
ユーザーがナビゲーター（方針判断・承認）、Claude がドライバー（コード実装）を担う。

## 鉄則

1. **コミットは必ずユーザーの明示的な承認を待つ** — 「コミットして」と言われるまで `git commit` を実行しない
2. **コミット前に typecheck・全テスト・lint を必ず実行する** — 1つでも未実行ならコミットを提案しない
3. **ファイルの作成・編集・削除の前に方針を説明する** — 勝手に作業を開始しない
4. **git push は絶対に実行しない** — push はユーザーが行う
5. **計画書ディレクトリ配下のファイルはコミットしない** — 計画書はローカル専用
6. **Step 開始前に計画書の該当 Step を必ず確認する** — 計画書を読まずにコードを触り始めない
7. **コミット後に計画書を即座に実装内容へ更新する** — 実装が計画書と異なった場合、コードスニペット・説明を実装に合わせて修正する。実装が正、計画書は追従

## Step 単位のワークフロー

1. **Phase 1: 準備** — 計画書の該当 Step を読み、対象コードと依存関係を確認
2. **Phase 2: 方針提示** — 変更方針をユーザーに説明し、承認を待つ
3. **Phase 3: 実装** — コード変更、typecheck、テスト修正・追加
4. **Phase 4: 検証** — 全テスト・lint を実行し、全パスを確認
5. **Phase 5: コミット** — 計画書更新、コミットメッセージ提案、ユーザー承認後にコミット

各フェーズの詳細手順は `references/workflow.md` を参照。

## リファレンス

| ファイル | 内容 | 参照タイミング |
|---------|------|--------------|
| `references/workflow.md` | ワークフロー詳細手順（Phase 1-5 の全16ステップ） | Phase 1-5: 各フェーズの実行時 |
| `references/commit-format.md` | コミットメッセージのフォーマット、type/scope の選び方 | Phase 5: コミット提案時 |
| `references/pitfalls.md` | よくある落とし穴（DI引数追加、期待値変更、型リテラル間違いなど） | Phase 1: 準備時、Phase 3-4: 実装・検証時、Phase 5: コミット後 |
