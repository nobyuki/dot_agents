---
name: verify-review
description: >
  GitHub PR のレビューコメントの妥当性を検証し、構造化された評価レポートを raw markdown で出力するスキル。
  ユーザーが PR レビューの URL を貼って「検証して」「妥当性を確認して」「レビューを評価して」と言ったときに使う。
  レビューコメントの URL（#pullrequestreview-xxx や #discussion_rxxx や #issuecomment-xxx）を含むリクエストが対象。
  Copilot、Claude Assistant、Codex など投稿者を問わず、PR に付いたレビューコメントであれば検証対象になる。
  主に自分が author の PR に付いたレビューコメントを検証する場面で使用する。
  自分以外が author の PR をレビューする場合は pr-review を使うこと。
---

# Review Comment Verifier

## 前提メモリ

スキル実行前に以下の Serena メモリを読み込むこと:

| メモリ名 | 用途 |
|---------|------|
| `project/plan_docs_location` | 計画書の保存先パス（設計判断の照合用） |

GitHub PR のレビューコメントに含まれる主張を、実際のコードとドキュメントに照らして検証し、構造化された妥当性評価を markdown で出力する。

## 評価の観点

- **事実の正確性**: 行番号参照、状態名、用語が実際のコードと一致するか
- **スコープ判定**: 指摘が当該 PR の変更範囲内か、境界上か、スコープ外か
- **問題発見力**: レビューが問題を自力で発見したのか、修正後のコードを見て「問題なし」と言っただけか
- **網羅性**: 指摘すべき類似箇所を漏れなくカバーしているか
- **断定の適切さ**: 主張の範囲が実際の検証範囲と一致しているか（過剰断定がないか）

## ワークフロー

1. **レビューコメントの取得** — URL パターンに応じて `gh api` で取得。投稿者・投稿時刻・種別を記録
2. **レビュー対象コードの特定** — 投稿時刻とコミットタイムラインを照合し、レビューが見たコードの時点を確定
3. **検証可能な主張の抽出** — 行番号参照、状態名、用語、ロジック整合性、網羅性の主張を列挙
4. **各主張の検証** — 当該コミット時点のコードと照合。断定表現は特に厳密に検証
5. **評価レポートの出力** — `/tmp/review-verify-{pr番号}.md` にファイルとして書き出し、パスをユーザーに伝える

各 Phase の詳細手順は `references/workflow.md` を参照。
レポートのテンプレートと評価基準は `references/report-template.md` を参照。

## 注意事項

- 複数のレビューコメントが指定された場合は、それぞれ独立して検証したうえで総合評価にまとめる
- あるレビューが別のレビューの評価（メタレビュー）の場合は、元のレビューも含めて検証チェーンを追跡する
- レビューの「時点」を常に意識する。修正前を見たのか修正後を見たのかで評価が大きく変わる
- 日本語のレビューには日本語で、英語のレビューには英語で評価を出力する
- レビューの妥当性検証が終わった時点で、検証過程で得た知見（新たな検証パターン、見落としやすいポイントなど）があればこの SKILL.md を更新する

## 過去の検証から得た知見

- **「見落とし」と断定する前に計画書を確認する**: レビューが言及していない事項を「見落とし」と評価する前に、計画書ディレクトリで意図的な設計判断として記載されていないか確認する。計画書に明記されている方針をレビューの欠陥として指摘すると、検証自体が不正確になる
- **「問題点」と「改善余地」を使い分ける**: 事実誤認は判定テーブルで `❌ 不正確` として扱う。レビューの観点不足・的外れな提案・網羅性の限界は「改善余地」セクションに記載し、「問題点」と断定しない。表現のトーンが強すぎると、検証レポートの信頼性を損なう
- **段階的更新時の整合性**: レポートを段階的に更新する場合（複数レビューの検証、ユーザーからの訂正反映）、解決済みの事項への参照が残りがちになる。更新のたびにレポート全体の整合性を確認する
