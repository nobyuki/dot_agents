# レポートテンプレート

## セクション順序（必須）

レポートは以下の順序で構成する。読み手が最初に結論と要対応事項を把握できることを最優先とする。

1. **概要** — PR のメタ情報
2. **総合評価** — Approve / Request Changes の判定、Findings 一覧テーブル、Security follow-up、フォローアップ推奨
3. **Findings 詳細** — 各 Finding の詳細説明
4. **PR 上のレビューコメント確認**（該当がある場合） — Copilot/Claude 等ボットレビューの指摘検証
5. **設計書との整合性チェック**（設計書がある場合） — 設計書の記述と実装の対応確認
6. **事実検証結果** — 各 Finding の主張に対する cross-verify 結果
7. **前回指摘の対応確認**（再レビュー時のみ） — 前回レビューの指摘への対応状況
8. **デグレチェック** — 既存機能への影響確認
9. **まとめ** — 残存 Finding の要約と推奨アクション

## 初回レビュー

```
# PR #{number} レビュー結果

## 概要

**PR**: {title}
**ブランチ**: `{head}` → `{base}`
**変更**: {files}ファイル (+{additions}/-{deletions})
**コミット**: {commits}件

## 総合評価

{Approve / Request Changes}。{理由の要約}

### Findings

| ID | 重要度 | カテゴリ | 概要 |
|----|--------|----------|------|
| {ID} | {Must Fix / Should Fix / Nit} | {Process / Correctness / Security / Performance / Maintainability / Documentation} | {概要} |

### Security follow-up (must create ticket)

（該当がある場合のみ。セキュリティ上の既存問題や、本 PR のスコープ外だが要チケット化の事項）

- {問題の概要。具体的なコード箇所と影響を明記}

### フォローアップ推奨

- **[Low]** {低優先度だが認識すべき事項}
- **[別スコープ]** {別 PR で対応すべき事項とチケットリンク}

## Findings 詳細

### {ID}: {タイトル} [{重要度} / {カテゴリ}]

{問題の説明。背景・影響・根拠を含める}

- **該当箇所**: `{file}:{line-range}`（{何のコードか}）
  - Process 系など該当箇所がない場合は省略可

**修正案**:

\```typescript
// 修正コード例（必要な場合のみ）
\```

> **補足**: {追加の文脈があれば}

#### Resolved Finding の表記

レビュー中に追加コミットで対応済みとなった Finding は、以下のパターンで記載する:

- Findings テーブル: `~~{概要}~~ → {N}th commit で resolved`
- 詳細セクションの見出し: `### {ID}: {タイトル} [{重要度} / {カテゴリ}] — resolved`
- 詳細セクション本文: 元の指摘を取り消し線で消し、対応内容を追記

```
### {ID}: {タイトル} [{重要度} / {カテゴリ}] — resolved

~~{元の指摘内容}~~

**{N}th commit ({sha}) で対応済み**: {対応内容の説明}
```

## PR 上のレビューコメント確認

（PR に Copilot/Claude 等のボットレビューがある場合のみ。該当なしなら省略）
（ID の採番規則は `references/finding-numbering.md` を参照）

| ID | レビュアー | 指摘 | Status | 検証結果 |
|----|-----------|------|--------|---------|
| {ID} | {Copilot / Claude} | {指摘の要約} | {open / resolved / merged / invalid} | {詳細} |

## 設計書との整合性チェック

（設計書・計画書がある場合のみ。該当なしなら省略）

| 設計書の記述 | 実装 | 整合性 |
|---|---|---|
| {設計書の仕様} | {実装状況} | OK / NG（{理由}） |

## 事実検証結果

本レビューの各主張について cross-verify を実施した結果。

| Finding | 事実判定 | 備考 |
|---------|----------|------|
| {ID} ({概要}) | {正確 / 不正確 / resolved} | {根拠} |

## デグレチェック

| チェック項目 | 結果 |
|------------|------|
| {確認した項目} | 問題なし — {理由} |

## まとめ

{残存 Finding の要約と推奨アクション。resolved 済みの Finding は対応済みである旨を簡潔に記載}

**件数の整合性**: まとめに記載する件数（「Should Fix N件、Nit M件」等）は Findings テーブルの実数と一致させる。外部レビュー指摘を Findings ではなくフォローアップ推奨に整理した場合は、その区別を明示する（例: 「Findings 本体は N件。○○ の指摘はフォローアップ推奨として別整理」）。件数の数え方が読み手に伝わらないと、本文との矛盾に見える。
```

## 再レビュー（Nth round）

```
# PR #{number} 再レビュー結果（{N}th round）

## 概要

前回レビュー（[issuecomment-{id}]({url})）の {前回Finding ID} に対する修正を検証。

| コミット | 内容 |
|---------|------|
| `{sha}` | {message} |

**変更ファイル**: {count}ファイル（{list}）

## 総合評価

**Approve / Request Changes**。{理由の要約}

### Findings

| ID | 重要度 | カテゴリ | 概要 |
|----|--------|----------|------|
| {ID} | {Must Fix / Should Fix / Nit} | {カテゴリ} | {概要} |

（新規の指摘がなければ「新規 Findings なし」と明記）

### フォローアップ推奨

- ...

## Findings 詳細

（新規 Findings がある場合のみ）

## 前回指摘の対応確認

| ID | 指摘 | Status | 確認箇所 |
|----|------|--------|---------|
| {前回ID} | {指摘の要約} | resolved | `{file}:{line}` |
| {前回ID} | ... | open | → Finding {今回ID} |

### 意図的にスキップ（PR作者判断）

| ID | 指摘 | スキップ理由 | 検証状況 |
|----|------|------------|---------|
| {前回ID} | ... | {PR作者の見解} | {コード事実としての検証結果} |

## {前回ID}（{タイトル}）の対応検証

**結果: 対応済 / 未対応 / 部分対応**

{検証の詳細。変更前後の比較、設計判断の評価を含める}

## 対応コメント（issuecomment-{id}）の検証

| 記載内容 | 実際 |
|---------|------|
| {コメントの主張} | {差分での確認結果} |

## デグレチェック

| チェック項目 | 結果 |
|------------|------|
| {確認した項目} | 問題なし — {理由} |
```
